# Lean/Lake build products
lean/.lake/
lean/build/

# TeX build products
build/
paper/*.aux
paper/*.log
paper/*.out
paper/*.synctex.gz

# Generated archives and checksums; keep only the rendered PDF in Git
output/*.tar.gz
output/*.zip
output/*.sha256

# Python and local temporary data
__pycache__/
*.py[cod]
tmp/
NUL

# Local tooling and unpublished notes; keep them out of GitHub/Zenodo
.dragonli/
notes/zhihu_*.md
